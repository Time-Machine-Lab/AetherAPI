## 1. 契约与复现

- [x] 1.1 阅读 `docs/api/api-credential.yaml`、`docs/sql/api_credential.sql`、Consumer & Auth 领域设计与后端开发规范，确认本变更不暴露 Consumer。
- [x] 1.2 复现 B3：创建 API Key 后调用 `disable` 与 `revoke`，记录真实异常栈和数据库状态。

## 2. 生命周期状态变更修复

- [x] 2.1 定位并修复 API Key 状态变更写路径中的聚合重建、状态规则、持久化更新或异常映射问题。
- [x] 2.2 确保 `disable`、`enable`、`revoke` 成功时返回更新后的状态，并且重新查询详情/列表能看到落库状态。
- [x] 2.3 确保重复停用、重复吊销、吊销后启用等冲突返回业务错误而不是默认 `500`。

## 3. 回归验证

- [x] 3.1 补充 API Key 生命周期应用服务测试，覆盖有效状态变更和冲突状态变更。
- [x] 3.2 补充 Web 层或集成测试，覆盖当前用户边界和错误响应。
- [x] 3.3 复测联调残留 Key 清理能力，确认后续测试数据可通过公开接口处理。
