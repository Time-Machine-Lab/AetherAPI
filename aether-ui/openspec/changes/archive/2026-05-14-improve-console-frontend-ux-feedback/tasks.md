## 1. 文档与契约边界

- [x] 1.1 确认本变更是否要实现更完整的调用日志诊断字段；如果需要，必须先通过 API 文档流程更新 `../docs/api/api-call-log.yaml`，再允许前端读取上游地址、请求体、响应体或 Header。
- [x] 1.2 实现前重新阅读 `../docs/spec/AetherAPI 前端技术栈与开发规范文档.md` 和 `aether-console/DESIGN.md`，确认本变更是否需要补充 DESIGN.md 中的可复用展示规则。
- [x] 1.3 确认 API 订阅功能继续保持在本变更范围外，除非已有独立订阅 API 与产品契约。

## 2. 共享 UI 组件

- [x] 2.1 新增或整理语义化元信息展示组件，包括图标元信息行、类型/状态/方法标签、可复制地址字段。
- [x] 2.2 新增可复用 JSON/代码展示组件，支持 JSON 格式化、纯文本兜底、复制反馈、只读与可编辑样式区分。
- [x] 2.3 为字段说明、复制反馈、诊断字段不可用状态、浮层标题和新增元信息标签补充 `zh-CN` 与 `en-US` i18n 文案。
- [x] 2.4 为格式化、复制辅助逻辑和组件工具函数补充必要单元测试。

## 3. 资产工作台体验

- [x] 3.1 将资产草稿创建重构为聚焦式模态框流程，并保留当前资产列表筛选与分页状态。
- [x] 3.2 将资产编辑重构为抽屉或模态框流程，并从选中资产详情填充表单。
- [x] 3.3 拆分或合并资产表单状态，确保绑定 AI 能力配置不会清空无关基础配置字段。
- [x] 3.4 为请求模板、请求示例、响应示例、上游地址、请求方法、鉴权方式、鉴权配置和 AI 能力字段增加说明。
- [x] 3.5 增强资产列表行，增加可扫读元信息、语义标签和清晰区分的行操作。
- [x] 3.6 在资产配置和上架流程中显式呈现分类依赖，包括分类管理不可见时的说明或导航。
- [x] 3.7 为创建/编辑浮层状态、AI 能力绑定状态保护和资产行操作补充回归测试。

## 4. 市场与统一调用引导

- [x] 4.1 增强 API 市场资产卡片，展示图标化发布者、发布时间、分类、请求方法和类型等可用元信息。
- [x] 4.2 在市场资产详情中增加可复制的平台统一调用地址，使用已契约化的 `/api/v1/access/{apiCode}` 路径。
- [x] 4.3 在资产拥有者详情中按需展示同样的平台调用地址，并清楚区分它和上游地址。
- [x] 4.4 增加从市场资产进入 Playground 的上下文传递，尽量预填 `apiCode`、请求方法和请求示例。
- [x] 4.5 在 Playground 中补充 API Key 使用说明，并为已登录用户提供进入 API Key 管理的路径。
- [x] 4.6 确保任何订阅相关占位能力都保持禁用，或明确标记为暂不可用。

## 5. 调用日志详情体验

- [x] 5.1 优化调用日志列表和详情布局，为方法、结果类型、成功状态、状态码、调用通道、凭证状态和 AI 扩展信息使用语义标签。
- [x] 5.2 使用可复用 JSON/代码展示组件展示 `usageSnapshot` 等当前契约已暴露的结构化字段。
- [x] 5.3 对当前契约未提供的上游地址、请求体、响应体、请求 Header、响应 Header 显示明确的不可用状态。
- [x] 5.4 为当前契约字段渲染和契约受限诊断占位补充测试。

## 6. 验证

- [x] 6.1 在 `aether-console` 运行 `pnpm lint`。
- [x] 6.2 在 `aether-console` 运行 `pnpm type-check`。
- [x] 6.3 在 `aether-console` 运行 `pnpm test`。
- [x] 6.4 在 `aether-console` 运行 `pnpm build`。
- [x] 6.5 在浏览器中手动验证资产工作台、市场详情、Playground 跳转、JSON/代码展示和调用日志详情流程。
