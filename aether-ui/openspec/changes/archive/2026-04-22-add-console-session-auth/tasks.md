## 1. 顶层契约与规范对齐

- [ ] 1.1 在开始实现前重新阅读 `../docs/spec/AetherAPI 前端技术栈与开发规范文档.md`、`../docs/api/console-auth.yaml`、`aether-console/DESIGN.md` 以及必要的架构文档，确认本次前端对接不需要先修改顶层权威文档。
- [ ] 1.2 如果实现阶段发现 `docs/api/console-auth.yaml`、统一前端规范文档或 `aether-console/DESIGN.md` 无法覆盖登录字段、会话恢复、401 反馈或表单语义，必须先更新对应顶层文档（适用时优先通过 `tml-docs-spec-generate` 生成规范化内容），再进入代码实现。

## 2. 控制台会话认证基础接入

- [ ] 2.1 在 `aether-console` 中新增与 `docs/api/console-auth.yaml` 对齐的认证 API 模块、DTO 与类型映射，封装控制台登录与当前会话查询请求，并归一化认证失败错误。
- [ ] 2.2 重构认证状态管理与会话编排，明确 `accessToken`、`currentUser`、初始化/恢复状态和本地持久化边界，移除本地 demo token 生成逻辑。
- [ ] 2.3 调整统一请求入口与路由守卫，使受保护路由在进入前完成会话恢复判定，并在控制台会话失效时统一清理状态和回跳登录页。

## 3. 登录页与控制台壳层对接

- [ ] 3.1 将 `sign-in` 页面改为真实登录表单，使用 `loginName` 与 `password` 调用后端登录接口，并补齐提交中、失败态和跳转后的反馈。
- [ ] 3.2 更新 `ConsoleLayout` 与相关受保护页面入口，使用户资料展示、退出登录动作和未登录跳转都基于真实控制台会话工作。
- [ ] 3.3 更新 `zh-CN` 与 `en-US` 国际化资源，移除“本地演示会话”文案，补齐登录失败、会话恢复、会话失效和鉴权边界说明。

## 4. 验证与联调保障

- [ ] 4.1 为认证 API 映射、路由守卫、登录成功/失败、刷新恢复与失效回退补充必要测试，覆盖高风险认证链路。
- [ ] 4.2 验证 `API Key`、`API 调用日志`、`调用工作台` 等受保护页面在真实控制台会话下的访问行为，确认控制台 bearer token 与 Unified Access API Key 语义仍然分离。
- [ ] 4.3 对 `aether-console` 执行 `lint`、`type-check` 与 `build`，并记录仍依赖后端联调环境的验证缺口。
