## 1. 设计文档同步

- [x] 1.1 更新 `aether-console/DESIGN.md`，新增控制台展示型组件视觉规范章节，覆盖元信息、标签、字段组、列表行、代码展示和状态反馈。
- [x] 1.2 在 DESIGN.md 中明确只读状态、可点击操作、字段输入、系统反馈、数据展示的视觉边界。
- [x] 1.3 确认本变更不需要更新 `../docs/api/*.yaml`，并记录无接口契约变更。

## 2. 展示型组件建设

- [x] 2.1 新增或整理元信息组件，支持图标 + 文本、图标 + 标签、图标 + 时间等表达。
- [x] 2.2 新增或整理状态/类型/方法标签组件，避免只读信息复用 Button 样式。
- [x] 2.3 新增字段组标题和增强 Label 组件模式，支持辅助说明、必填/可选提示和视觉强调。
- [x] 2.4 新增结构化列表行组件模式，用于资产、日志、API Key 等高密度信息列表。
- [x] 2.5 新增 JSON/代码展示组件，支持格式化、纯文本兜底、复制反馈和可选折叠。
- [x] 2.6 新增或统一空态、错误态、不可用态和加载态展示组件。
- [x] 2.7 为新增组件补充 `zh-CN` 与 `en-US` i18n 文案。
- [x] 2.8 为 JSON 格式化、复制反馈、状态变体等逻辑补充单元测试。

## 3. 页面视觉升级

- [x] 3.1 使用新元信息和标签组件升级 API 市场资产卡片。
- [x] 3.2 使用字段组、元信息和代码展示组件升级 API 市场详情面板。
- [x] 3.3 使用结构化列表行和语义标签升级资产管理列表。
- [x] 3.4 使用字段组和增强 Label 组件升级资产配置表单。
- [x] 3.5 使用状态标签、元信息和诊断分组升级调用日志列表与详情。
- [x] 3.6 使用字段组、代码展示和结果状态组件升级 Unified Access Playground。
- [x] 3.7 检查 ConsoleLayout 中的顶部工具、搜索、通知和侧边栏，避免组件表达与新规范冲突。

## 4. 视觉一致性与响应式检查

- [x] 4.1 检查桌面视口下市场、资产、日志、Playground 的信息层次和视觉密度。
- [x] 4.2 检查移动视口下卡片、列表行、代码块和字段组不溢出、不重叠。
- [x] 4.3 检查只读状态没有 hover lift、cursor pointer 或按钮阴影等操作暗示。
- [x] 4.4 检查可点击操作均具备 hover、focus、disabled 和 active 状态。

## 5. 验证

- [x] 5.1 在 `aether-console` 运行 `pnpm lint`。
- [x] 5.2 在 `aether-console` 运行 `pnpm type-check`。
- [x] 5.3 在 `aether-console` 运行 `pnpm test`。
- [x] 5.4 在 `aether-console` 运行 `pnpm build`。
- [x] 5.5 通过浏览器人工验证 API 市场、资产管理、调用日志和 Playground 的视觉组件表现。
