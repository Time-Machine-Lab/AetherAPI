## 1. 权威上下文与入口整理

- [x] 1.1 重新阅读 `../docs/spec/AetherAPI 前端技术栈与开发规范文档.md`、`../docs/api/api-call-log.yaml` 与 `aether-console/DESIGN.md`，确认本次实现无需先更新顶层权威文档。
- [x] 1.2 在 `aether-console` 控制台导航中增加 API 调用日志入口，并确定其在受保护工作区中的 hash 分区与激活态规则。
- [x] 1.3 将 `console-workspace` 的分区装配逻辑整理为可扩展结构，确保日志分区接入后不会破坏现有 `credentials`、`catalog-manage` 与 `category-manage` 行为。

## 2. 日志查询数据层

- [x] 2.1 新增 API 调用日志查询模块、DTO 与类型映射，严格对齐 `docs/api/api-call-log.yaml` 中的列表参数、分页结果与详情结构。
- [x] 2.2 实现日志工作区所需的页面级查询编排逻辑，包括分页、`targetApiCode` 筛选、时间范围筛选、当前选中日志与详情加载。
- [x] 2.3 实现日志列表与详情的错误处理边界，确保无效查询、详情失败和不可查看场景均按契约反馈，不推导未返回信息。

## 3. 控制台日志工作区界面

- [x] 3.1 实现 API 调用日志工作区的筛选条、分页列表与桌面端列表/详情双区布局，并保持与现有控制台 Surface、Field、Status 语义一致。
- [x] 3.2 实现日志详情区域，按字段可用性展示摘要信息、错误对象与 AI 扩展字段，并为空态、加载态和错误态提供清晰反馈。
- [x] 3.3 为移动端补齐收敛布局，确保 API 调用日志页面在小屏下仍可完成筛选、选中与详情查看闭环。

## 4. 文案与验证

- [x] 4.1 补齐 API 调用日志相关 i18n 文案，覆盖标题、筛选、空态、错误反馈和详情说明，不硬编码最终用户可见文本。
- [x] 4.2 为日志查询编排与关键页面状态补齐测试，至少覆盖列表加载、筛选刷新、详情加载和失败反馈。
- [x] 4.3 运行并通过 `lint`、`type-check`、相关测试与 `build`，确认日志分区接入后不回归现有控制台能力。
