## 1. Catalog API 集成

- [x] 1.1 为 Discovery、资产管理和分类生命周期契约新增 `aether-console` API 模块、DTO 与类型定义
- [x] 1.2 新增特性层辅助逻辑，用于格式化资产 / 分类数据、AI 能力标签与最近资产快捷入口，且不引入新的全局 Store

## 2. 市场体验

- [x] 2.1 将 `console-home` 当前的市场骨架替换为基于 Discovery 的列表加载、搜索 / 筛选控件与摘要卡片
- [x] 2.2 新增按需加载的资产详情渲染，覆盖 AI 能力、示例快照、加载、空态和错误态
- [x] 2.3 更新 `ConsoleLayout` 导航与控制台顶层文案，使公开页面语义与 API Hub 市场保持一致

## 3. 受保护的管理工作台

- [x] 3.1 将当前 `console-workspace` 占位面板替换为受保护的 API Catalog 工作台布局
- [x] 3.2 实现分类管理动作：列表、创建、重命名、启用与停用
- [x] 3.3 实现资产注册、`apiCode` 查询、修订、启用 / 停用与 AI 能力档案绑定流程
- [x] 3.4 提供清晰的“最近打开资产”入口，并明确标注其为本地快捷方式而非完整服务端清单

## 4. 本地化与验证

- [x] 4.1 重写 `aether-console` 在 `zh-CN` 与 `en-US` 中的多语言资源，使其对应 API Hub 市场与工作台文案
- [x] 4.2 对 `aether-console` 执行 `lint`、`type-check` 与 `build`，并修复验证过程中发现的回归问题
